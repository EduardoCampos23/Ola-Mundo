# frase = "O Python é uma longuagem de programação " \
# "multiparadigma."\
# "Python foi criado por Guido van Rossum."

# i = 0
# while i < len(frase):
#     letra_atual = frase[i]
#     qts_x_letra_apareceu = frase.count(letra_atual)

#     print(letra_atual, qts_x_letra_apareceu )
    # i += 1

frase = "O Python é uma longuagem de programação " \
"multiparadigma."\
"Python foi criado por Guido van Rossum."

i = 0
while i < len(frase):
    letra_atual = frase[i]
    qts_x_letra_apareceu = frase.count(letra_atual)

    print(letra_atual, qts_x_letra_apareceu )
    i += 1